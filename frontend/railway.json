{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "start": {
    "cmd": "npm start"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE"
  }
}
